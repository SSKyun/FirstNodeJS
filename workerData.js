/* const {
    Worker, isMainThread, parentPort, workerData,
  } = require(

let { workerData } = require("worker_threads");

const data = workerData
parentPro.postMessage(data.start + 100+ data.msg);
// parentPort.won(); 처리하지 않아서 postMessage()처리후 lcose()ㅏ
 */